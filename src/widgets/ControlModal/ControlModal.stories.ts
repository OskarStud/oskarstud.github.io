import type { Meta } from '@storybook/react';
import { ControlModal } from './ControlModal';

const meta: Meta<typeof ControlModal> = {
  title: 'ControlModal',
  component: ControlModal,
};

export default meta;
export const Default = {};
